<!--pages/wxml/wxml.wxml-->
<!-- 1.wxml的格式 -->

<!-- 2.Mustache语法 -->
<view>{{message}}</view>
<view>{{firstname}} {{lastname}}</view>
<view>{{firstname + ' ' + lastname}}</view>
<view>{{age >=18 ? '成年人' : '未成年人'}}</view>
<!-- 时间的获取方法变了 -->
<view>{{time}}</view>

<button size='mini' bindtap='handleSwitchColor'>切换颜色</button>
<view class='box {{isActive ? "active" : ""}}'>哈哈哈</view>

<map longitude='111.11' latitude="39.6">111</map>

<view>------------条件判断-----------</view>
<!-- wx:if的使用 -->
<button size="mini" bindtap='handleSwitchShow'>切换显示</button>
<view wx:if="{{isShow}}">hahah</view>


<!-- wx:elif/wx:else -->
<button size='mini' bindtap="handleIncrement">分数递增</button>
<view wx:if="{{score >= 90}}">优秀</view>
<view wx:elif="{{score >= 80}}">良好</view>
<view wx:elif="{{score >= 60}}">及格</view>
<view wx:else>不及格</view>

<!-- hidden属性 -->
<view hidden='{{false}}'>我是hidden属性控制的内容</view>

<!-- wx:if和hidden在隐藏组件时有什么区别 -->
<!-- 
    hidden:将一个组件隐藏起来是，该组件依然是存在的的
    wx:if:将一个组件隐藏起来时，该组件压根不存在（压根没有创建）
选择：
    如果显示和隐藏切换的频率非常高，选择使用hidden
    如果显示和隐藏切换的频率非常低，选择使用wx:if
 -->
<view>----------------------</view>
<view hidden='{{true}}'>我是hidden属性控制的内容</view>
<view wx:if="{{false}}">我是wx:if属性控制的内容</view>


<view>----------列表渲染------------</view>
<!-- 1.wx:for的回顾 -->
<!-- 1.1遍历数组/字符串/数字 -->
<view wx:for="{{['abc','cba','nba']}}">{{item}} {{index}}</view>
<view wx:for="tellmewhy">{{item}} {{index}}</view>
<view wx:for='{{9}}'>{{item}}</view>

<!-- 2.block标签 -->
<block wx:if="{{false}}">
  <button>按钮</button>
  <view>呵呵呵</view>
  <text>我是内容</text>
</block>
<block wx:for="{{3}}">
  <button>按钮</button>
  <view>呵呵呵</view>
  <text>我是内容</text>
</block>
<!-- 3.item\index起名字 -->
<view wx:for="{{movies}}" wx:for-item="movie" wx:for-index="i">{{movie}} {{i}}</view>

<block wx:for="{{nums}}" wx:for-item="innernums">
  <block wx:for="{{innernums}}">
    <view>{{item}}</view>
  </block>
</block>

<!-- 4.key的作用 -->
<!-- 如果用index仅仅是消除警告 -->
<view class='container'>
  <view wx:for="{{letters}}" wx:for="{{item}}">{{item}}</view>
</view>

<view>------------template------------</view>
<!-- 
之前的时候小程序不支持自定义组件，为了进行代码的复用：template
 -->
 <!-- 模板中包裹的内容，在没有被使用前，是不会进行任何渲染的 -->
 <!-- <template name="con_templete">
  <button size="mini">{{btnText}}</button>
  <view>{{content}}</view>
 </template> -->

 <!-- 
 关于wxmld的import有两种方式：
 import导入：
  1.主要是导入templete
  2.特点：不能进行递归导入
 include引入
  1.将公共点wxml中的组件抽取到一个文件中
  2.特点：不能导入template/wxs，可以进行递归导入
  -->
<import src="/pages/wxml/templete.wxml"/>
<template is="con_templete" data="{{btnText: '按钮', content:'哈哈哈'}}" />
<template is="con_templete" data="{{btnText: '警告', content:'嘻嘻嘻'}}" />
<template is="con_templete" data="{{btnText: '点击', content:'嘿嘿嘿'}}" />
<template is="con_templete" data="{{btnText: '登录', content:'呵呵呵'}}" />

<!-- 错误的写法 -->
<!-- <view>{{25.666666666.toFixed(2)}}</view> -->







